import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.alarmaRobo.starter',
  appName: 'alarmaRobo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
